- hosts: all
  gather_facts: False
  vars_prompt:
    - name: "Username"
      prompt: "Enter the username for this container?"
      private: no

  tasks:
    - name: Ansible prompt example.
      debug:
        msg: "{{ Username }}"
